# Node.js dependencies
node_modules/

# Compiled output
dist/

# Environment variables (.env = local/Tyril by default; .env.production = live
# Tosche config, loaded locally via ENV_FILE=.env.production)
.env
.env.*

# Logs
*.log

# TypeScript
*.tsbuildinfo

# package-lock.json IS committed (reproducible installs on Sparkedhost) — not ignored.

# Reference-only legacy bot (never imported; contains a burned-but-real MongoDB
# credential in bot.js — must never enter git history).
OldBot/

TODO.md

# Personal, beginner-friendly guide (Polish) — not for GitHub
PRZEWODNIK.md